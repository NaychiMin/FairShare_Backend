version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: fairshare-mysql-sit
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-fairshare}
      MYSQL_USER: ${MYSQL_USER:-fairshare}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-fairsharepass}
    ports:
      - "3307:3306"
    volumes:
      - fairshare-mysql-sit-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fairshare-network

  backend:
    image: ghcr.io/naychimin/fairshare_backend:latest
    container_name: fairshare-backend-sit
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: sit
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/fairshare?allowPublicKeyRetrieval=true&useSSL=false
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-fairshare}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-fairsharepass}
      # Validates schema and runs data.sql automatically
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      SPRING_JPA_DEFER_DATASOURCE_INITIALIZATION: "true"
    ports:
      - "8081:8080"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fairshare-network

volumes:
  fairshare-mysql-sit-data:

networks:
  fairshare-network:
    driver: bridge
